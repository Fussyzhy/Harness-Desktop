# Application Icon

Place the Harness Desktop application icon at `build/icon.png`.

Use a square PNG with transparency. A 512 x 512 image is recommended; the
minimum supported size for Windows packaging is 256 x 256.

The image is used for the development window, packaged executable, installer,
uninstaller, and application shortcuts.
